<?php

// Toán tử số học
// $num = 5;
// echo $num + 3; // 8
// echo $num . 3; // 53

// "+" : cộng toán học ép kiểu dữ liệu của biến về dạng số thực hiện cộng toán học
// "." : cộng chuỗi - ép kiểu dữ liệu của biến về dạng chuỗi thực hiện ghép 2 chuỗi vào với nhau 5.3 => 53
// "-"
// "*"
// "/"
// "%": 5%2 = ??? => 1

// += 
// -= 
// *= 
// /= 
// %= 
// $x = 6;
// $x += 2;
// echo $x;

// ++ | $x++ | ++$x
// -- | $x-- | --$x

// $x = 5;
// echo (((++$x+3)-$x--)-5);
// echo "<br>";
// echo $x;

/** toán tử so sánh
    == so sánh bằng (giá trị)
    === so sánh tuyệt đối (giá trị và kiểu dữ liệu)
    != so sánh khác
    !=== khác tuyệt đối

    > 
    >=
    <
    <=
 */

 var_dump(true === 1);
?>